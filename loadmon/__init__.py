"""loadmon — отдельный проект-монитор нагрузки ПК и локальных моделей.

Самодостаточный FastAPI-сервис (по умолчанию порт 8770), который:
  • снимает нагрузку: CPU/RAM (psutil), GPU/VRAM/температура (nvidia-smi),
    загруженные модели Ollama (HTTP /api/ps);
  • пишет историю в свою SQLite (loadmon.db);
  • отдаёт API (/api/load/now, /api/load/history, /api/models) и дашборд (/);
  • принимает события генерации от Persona (POST /api/load/event) — токены/с,
    латентность по моделям — чтобы потом сравнивать и оптимизировать.

Persona (порт 8000) дергает этот API для индикатора нагрузки в чате при
локальной модели. CORS открыт для localhost.
"""

__version__ = "0.1.0"
