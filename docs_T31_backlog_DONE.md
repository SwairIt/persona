# T31 — большой backlog (автономный прогон). Удаляю пункты по мере выполнения.

## Решения пользователя (фиксированные)
- Агентное выполнение команд (bash/powershell) — НА MAC через агента; режимы гейтят доступ.
- Голосовой ассистент: озвучка на Mac системным голосом (macOS `say`); wake-word «Персона».
- Продвинутый чат — ГИБРИД: эффорт/режимы переключают модель (локальная Ollama ↔ облачный Gemini).
- Новая тема кабинета — ФИОЛЕТОВАЯ по умолчанию (в стиле лендинга).
- Порядок: п.1 авторизация → п.2 кабинет → п.3 span → п.4 чат → голос. Коммит+пуш+рестарт после каждой задачи.
- Bypass permissions включён. Боевой сервер: uvicorn :8000, рестарт через Stop-Process по app.web.main + Start-Process (lean, env PERSONA_DATA_DIR). watchdog поднимает сам через ~3 мин.
- Владелец user_id=2 (yorikoneno@gmail.com). Тест входа в браузере: создать magic-link через app.auth.magic.create_magic_link, открыть /auth/magic/<token>.
- Версия: бампать app/__init__.py + sw.js каждый коммит.
- В конце: время окончания по МСК + число строк кода, выключить луп.

## ФАЗА A — Авторизация (п.1) — ГОТОВО (commit, версия 2.11.0)
(inline magic+пароль+забыли пароль на лендинге, JSON-эндпоинты, /auth/forgot,
 /auth/set-password, кнопка «Войти» в шапке открывает inline-форму. Проверено curl.)

## ФАЗА B — ГОТОВО (commit 2.11.1)
(/apps был 500 — TemplateSyntaxError в apps_index.html:28 (кривое экранирование),
 починен через |tojson. «Граф» (/graph) добавлен в основное меню (i18n nav_graph
 ru/en/de). «Лента»/timeline уведена в «Ещё». Логотип оставлен → / (стандарт).)

## ФАЗА C — ГОТОВО (commit 2.12.0)
(подсветка фрагмента mark.span-like/span-dislike зелёным/красным + title-подсказка;
 renderMsg() оборачивает оценённые фрагменты; get_span_ratings грузит в страницу;
 оптимистичная подсветка при клике. Проверено: rate-span ok, span_ratings в странице.)

## ФАЗА D — ГОТОВО (commit 2.13.0)
(тема persona: persona_theme.css (.theme-persona) — градиентный фон + стекло панелей;
 whitelist+дефолт в templates_engine.py и theme.py; base.html html class; опция в
 /settings/theme; kv theme=persona. Проверено Playwright: /now читаемо, 0 ошибок.)

## ФАЗА E — Чат: фишки как у Клода (п.4) — крупное
- [x] E1. ИИ знает, что он модель Persona — ГОТОВО (2.13.1): _PERSONA_IDENTITY префикс в _base_prompt.
- [x] E2. Эффорт-селектор — ГОТОВО (2.13.3): kv chat_effort_{sid}, дропдаун Быстро/Норма/Глубоко, маппинг на max_tokens+temperature в send-stream+send.
- [x] E3. Режимы работы — ГОТОВО (2.13.4): plan/ask/auto/bypass, kv chat_mode_{sid}, гейт инструментов (plan/ask не выполняют tools), промпт-хинт, дропдаун+setMode.
- [x] E4. ГОТОВО (2.13.8): инструмент run_mac (op=exec через агента, миграция 174 включает), блоки IN/OUT карточками с кружком-статусом (pending/running/done/error), бейдж шелла, длинный вывод сворачивается сверху И снизу. Реальное выполнение на Mac (op=exec у агента) — нужна проверка на Mac; офлайн не ломает.
- [x] E5. Авто-промпты — ГОТОВО (2.13.5): app/chat/auto_prompts.py (7 типов по триггерам), kv auto_prompt, накладка в send-stream, тумблер 🧩 в чате.
- [x] E6/E8(меню). ГОТОВО: меню выбора ответов (persona:choices) — варианты + «рекомендую» + своё поле снизу; выбор уходит промптом.
- [x] E6. MCP — ГОТОВО (2.13.6): инструмент install_mcp (миграция 173 включает его), ИИ ставит MCP по просьбе; страница /admin/mcp причёсана (баннер про install через чат).
- [x] E8(скиллы). ГОТОВО (2.13.7): скиллы зеркалятся файлом в папку на устройстве (kv skills_dir), best-effort через Mac-агента.
- [x] E9. ГОТОВО (2.13.9): чат «живой» — умный авто-скролл (не дёргает, если читаешь выше), кнопка «вниз», плавное появление сообщений, перегенерация 🔄, копирование 📋, сохранение черновика в localStorage. Индикатор «думает», стоп, авто-resize, Enter/Shift+Enter — уже были.

## ФАЗА F — Голосовой ассистент (последнее)
- [x] F1. ГОТОВО (2.14.0): /settings/voice — вкл/выкл + выбор чата + голос say.
- [x] F2. ГОТОВО (2.14.0): POST /api/voice/utterance — отрезает «Персона», ответ в выбранном чате, в очередь TTS. Детект на агенте — нужна проверка на Mac.
- [x] F3. ГОТОВО (2.14.0): очередь voice_tts + GET /api/voice/pending (команда say) + ack. Проигрывание say — на Mac (нужна проверка на Mac).

## Финал
- [x] Финал: отчёт сделан, луп выключен.
