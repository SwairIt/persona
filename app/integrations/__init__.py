"""Adapters for external services.

The integration packages translate a third-party protocol into Persona
application calls.  They must not expose provider credentials to the domain
layer or duplicate the underlying chat/memory storage.
"""
